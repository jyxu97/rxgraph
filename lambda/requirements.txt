# Lambda container dependencies
# boto3 is pre-installed in the Lambda runtime but pinning it ensures the same
# version runs locally when testing with docker run --env-file .env.
neo4j==5.19.0
openai>=1.52.0
requests==2.32.3
boto3>=1.34.0
python-dotenv>=1.0.0
